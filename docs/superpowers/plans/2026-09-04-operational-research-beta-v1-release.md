# Relay Rider Operational Research Beta v1 Release Sync

This temporary operations branch exists only to synchronize current `main` into PR #52 before release verification. It adds no product functionality and must not be treated as the release candidate itself.
